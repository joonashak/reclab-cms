version: "3.2"

services:
  app:
    build: .
    ports:
      - 3001:3001
    container_name: reclab-cms
    environment:
      - NODE_ENV=development
      - PORT=3001
    volumes:
      - ./:/usr/src/app
